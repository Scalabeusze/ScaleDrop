spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: 'jdbc:tc:postgresql:15.2-alpine:///postgres?TC_DAEMON=true&currentSchema=sd_iam'
    username: postgres
    password: password
    name: sd_iam
